# e732f2b 第二轮修复记录

基线：e732f2b4ef46e0ac482480095a31cb5aa23ec6be；Node 24.14.0。

## 阶段一：P1 语义

- 共用编码相容关系：支持区非空交集即相容（不宣称分辨率内可分辨）；完全相同支持区为别名。同来源别名以每次观察等权均值聚合，保存样本数/最小/最大值；仅单获胜核且实际输出激活支持区完全相同时细化神经读出，空/多峰答案不被填充。
- 观察优先于假设、同来源代际更新；冲突假设跳过写边，登记冲突事件。supersede 与条件替代档抑制分离，独立来源贡献在 Γ 饱和时仍可撤销；Γ 保持对称保守项。
- 联合捕获按全部观察维检验相容性；预测缓存值快照、证据代际和动力学状态。新条件的矛盾观察还学习条件差分成员到冲突核的抑制（共享条件成员不受影响）；这是关联模式分离，不新增因果因素声明。结果相容后撤销相应贡献。
- 静息候选资格贯穿退火和淬火；无合法候选返回 no-quiet-candidate、空模式、converged=false。
- R2 场级差分覆盖两侧通道并集，保留反对称符号。

验证：新增永久阶段一集 22/22（runs/review2/stage1-final.tap）；评审甲 P1 选择集 8/8。
评审乙原 N28 存在测试顺序问题：同一模型纠错后仍要求下一种子预测旧错误值 .2。原脚本保留，永久测试每种子独立模型并增断言反馈后 .8；不得把原脚本结果冒写通过。
完整基准和全量原评审复跑留到阶段四；当前并未宣称全部验收完成。

## 阶段二：输入、写入与动力学契约

- 公共写入先完成 schema/有限数/整数、完整编码、容量预检，再进入无校验失败点的提交段；非法请求不改变签名表、核游标、证据代际或边。Hebbian 全批次先验证全部端点。
- Field 核互斥基于全部已分配核，登记顺序不影响布局。生命周期为已分配→已有结果证据→已登记→可预测，空结果拒绝。
- levels/sweeps 为正整数；候选/势阱成员为合法整数 id；温度有限正数；零淬火预算在翻转前检查；DI cap 非负。离散实验台拒绝小数档，传感器拒绝 NaN。
- Explorer/ContinuousExplorer 的日志保留 converged、terminationReason、任一输出拒答；只有收敛预测进入置信连续验证。注意力缓存同样携带值快照、代际和动力学状态。
- 官方模型基准日志单列全部 predict 调用的非收敛率、任一输出拒答率、终止原因分布；外部答案质量另行评分。诊断计数不参与学习或随机数。

定向实测：评审甲接口集 8/8，评审乙接口集 15/15；永久回归累计 52/52，见 stage2-final.tap。阶段一早期全量运行 113/113（455.5 秒）仅对应该次启动源码，不能替代阶段四最终复验。

## 阶段三：评估与治理

- 2×2 消融以 R2 的 boost 键集合表示因素资格，gain=0 只关 W 加分，veto 单独控制 Γ。`runner-ablation.ts` 实测四格 W/Γ 为 (0.4,0)、(0.4,1.5)、(1.2,0)、(1.2,1.5)。
- planner 返回历史/配对深拷贝；Field 规则核和结果支持区返回副本。
- Pop 构造检查饱和点火可行域：`min(0.6,maxWeight) × (coreSize+popSize−1) >= θ`，条件侧和结果侧均检查。这里 cap 是绝对边权（单位已含 η×repeats），不能再乘 η；对计划文本的 η·cap 记法作此单位澄清。默认 4/4 可行，1/1 不可行而显式拒绝。
- quorum-met 为启发式停止，无完备发现保证。W04 稀有合取保留为覆盖风险样例；同预算基线与阳性召回在阶段四报告。
- `clusterThresholdRatio` 构造配置已真正生效并验证范围；heldout n=0 显示 N/A。
- 定论：概念重形成后的 bindInfluence 是有意的额外训练 epoch，不是幂等更新、不是新增独立证据。R2 统计先清零重算，R3 有界增量学习；更新/冻结对照必须披露训练次数差别。
- 连续网格外泛化没有稳定超过同观察最近邻；作为能力边界保留，不能由网格分数推断普遍优势。

阶段三永久回归 57/57；评审甲本轮原样新增集 20/20。完整基准数值以随后阶段四输出为准。

## 全量复验发现的后续修复（保留失败）

严格静息回退的第一版让 W01 出现 0/16 读回：合法候选由多个核的零散成员组成。只增加静息单点/交换淬火仍只有 3/16，因为结果群体有协同点火势垒，零温单点不能跨越。两次失败均保存在 `runs/review2/initial-failed-run/`，未用增加 repeats、提高兴奋上限或世界专用阈值掩盖。

最终处理：静息模式的 Metropolis 在同一合法域内提议单点翻转；若激活会招募 DI，则提议与一个活跃自由成员交换。合法态间的热涨落保留跨协同势垒能力；随后单点/交换淬火严格使用原翻转预算。全程只读局部能量与 DI 资格，不读取规则标签或真值。域内停止若仍有无约束残余翻转，报告 quiet-constraint、converged=false，绝不伪称固定点。

W01 因而恢复 16/16，四个正常独立世界保持准确率与因素检查通过；W01 的 16 次预测仍全部非收敛，答案质量和动力学质量的差别必须保留。连续探索原先硬编码 192 个核，小于 300/400 的实验预算；严格置信策略暴露了这个容量不匹配，现以配置预算预留核，单独记忆实例的显式容量边界不变。

局部模式分离进一步限定为条件支持区重叠且结果冲突的经验对；远隔经验不增加这类抑制，避免破坏原 F05 的相邻未训练输入读出。补测：新旧核心回归 75/75；完整对象原子性、抑制来源隔离、clusterThresholdRatio 实效性 3/3。

## 语料形成与置信停止解耦

全量运行还暴露了 B0 的循环依赖：要形成概念却先要求尚未形成概念的预测连续收敛。现以至少 corpusQuorum 条真实观察且每个条件维至少两个不同值作为表示更新的充分性门；这不是预测置信或因素完备证书。B2 的置信停止仍须连续 converged=true。量程扩展采用预先声明的对半预算：第一阶段最多 200 次，扩展后总预算 400，两种模式一致，与得分无关。旧的“必须预算前自然停止”测试契约撤回，替换为预算上限、真实终止原因及 quorum-met 必须具有连续收敛验证；因素和门控准确率断言保留。

为完成全量实验，求解器内部改用按神经元编号排序的活跃成员视图，跳过零项而保留原求和顺序。与 1b25157 稠密版本比较 200 组随机网络，完整返回对象（能量、状态、轨迹、计数器、终止原因）逐项严格相等，记录见 sparse-solver-equivalence.json。未完成旧运行原样归档到 pre-corpus-fix-interrupted，明确标记中断，不能算通过证据。
